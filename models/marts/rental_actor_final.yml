version: 2
models:
  - name: rental_actor_final
    description: Tabela final de aluguel por ator
    columns:
      - name: rental_actor_pk
        description: Chave primária da tabela rental.
        tests:
          - unique
          - not_null